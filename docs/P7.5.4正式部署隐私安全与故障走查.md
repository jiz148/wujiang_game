# P7.5.4 正式部署、隐私安全与故障走查

> 走查日期：2026-08-06  
> 环境：Windows Docker Desktop，Linux `python:3.12-slim-bookworm` 应用容器，隔离命名卷  
> 结论：本地 Docker 部署、生产工程门禁、隐私安全审计与故障走查全部通过；P7.5.4 已完成

## 构建与正常链路

- `compose.local.yml` 真实构建并启动成功。由于本机已有 `wujiang_api` 占用 8000，本次通过 `WUJIANG_LOCAL_PORT=8001` 运行；容器只绑定 `127.0.0.1:8001`，Docker 状态为 healthy，首页、live、ready 均返回 200。
- 本地 `wujiang_local_data` 与 `wujiang_local_replays` 命名卷分别挂载到 `/app/var` 和 `/app/replays`；默认端口仍为 8000，可通过环境变量安全改用其他本机端口。
- Compose 配置解析通过。应用 8000 只暴露到私网，对外只有 Caddy 80/443。
- 真实构建 `wujiang:p7.5.4` 通过；基础镜像解析摘要为 `sha256:4766d8b510c428e595d74b9cc5bbb2fae8e26316fffb4adc89908d79aacd58a2`。
- 容器以 UID/GID 10001、只读根文件系统、限额 tmpfs、去除 capabilities 和两个持久卷运行。
- 生产启动预检通过；非 HTTPS 公网地址、不匹配 Origin、查询令牌开启、缺失/占位运维 token 或关闭结构化日志均在监听前失败。
- live 返回 200/ok，ready 初始化四个依赖后返回 200/ready。生产 Origin 下注册成功，落库只保存 session SHA-256 摘要和非敏感占位值。
- 手工发布前备份为 163,840 bytes，清单版本 1、战略结构版本 3、存档版本 1，摘要与隔离恢复演练通过。
- 最终生产审计检查 4 个数据库、2 个 session、分析/战绩/战略审计/回放脱敏、战略完整性、1 份已验证备份和隔离恢复演练；结果 passed，critical=0，warning=0。

## 故障注入

- **维护门禁**：写入 maintenance marker 后，live 保持 200/ok，ready 降为 503/not_ready；清除标记后 ready 恢复 200。
- **损坏备份**：注入无清单且字节改动的 SQLite 副本后，战略诊断降为 warning，生产审计升格为 critical，返回码 1 并阻断发布。删除故障副本后审计恢复零警告。
- **镜像升级/进程重启**：删除旧容器、保留数据/回放卷并用重建镜像启动。ready 为 200，原账号成功登录，最新备份仍可演练恢复。
- **优雅停机**：`docker stop --time 10` 发送 SIGTERM，HTTP shutdown 完成，容器退出码为 0。

## 隐私与日志

- 专项测试故意向分析 JSON 与回放 JSON 注入凭据字段，审计同时报出 `analytics_contains_credentials` 和 `replay_contains_credentials`，但不回显原始秘密。
- 容器日志扫描不包含测试账号、密码、session 字段或运维 token，只保留路由模板、状态、耗时、请求 ID 和限流分类。

## 可选公网部署

本阶段的交付标准调整为本地 Docker 部署。服务通过 `compose.local.yml` 仅绑定 `127.0.0.1:8000`，数据与回放保存在命名卷中；代码、容器、审计和故障走查均已通过，因此 P7.5.4 与 P7.5 完成。真实公网主机、域名、DNS 和 TLS 发布尚未执行，其方法保留在 [正式部署与回滚手册.md](C:/Users/jiz14/TeamGH/wujiang_game/docs/正式部署与回滚手册.md)，未来需要时再按该手册执行，不作为当前阶段门禁。
